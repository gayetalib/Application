{% extends "base.html.twig" %}

{% block css %}

  <link rel="stylesheet" href="{{ asset('stockCSS/form.css') }}"> 
  
{% endblock %}

{% block body %}
{{ form_start(form) }}
<div class="container" id="contenu" style="margin-top: -35px;margin-left: 200px">
    <div class="row">
        <div class="col-md-5">
            <div class="panel panel-primary">
                <div class="panel-heading">
                     <h3 class="panel-title" style="padding-left:110px; font-size:25px">Ajouter Type d'article</h3>

                </div>
                
                <div class="panel-body">
                    <form class="form-horizontal" role="form">
                            <div class="form-group">                 
                                           {{ form_row(form.NomType,{ 'attr' : { 'class' : 'form-control', 'label' : 'GAYE'}}) }}
                                
                                          </div>
                                        
                    
                      <div class="input-group ">
       
        <div class="input-group-btn">
         
          </button>
        </div>
      </div>
      <div class="form-group">
                            <div class="col-sm-offset-2 col-sm-10">
                                <button type="submit" class="btn btn-default" style="background-color: royalblue;color:aliceblue;margin-left:75px;font-size:20px">Valider</button>
                            </div>
                        </div>
                    </form>
                    <!-- Flash Message -->
                    <div class="col-md-12 col-lg-12 col-xl-12"> 
                        {% for message in app.flashes('info') %}
                         <div class="alert alert-success" style="font-size: 25px;padding-left:100px" id="alert">
                               <strong>{{ message }}</strong> 
                            </div>
                        {% endfor %}
                    </div>

                  
                        {% for label, messages in app.flashes %}
                            {% for message in messages %}
                                <div class="alert alert-{{ label }}" role="alert">
                                    <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
                                    {{ message }}
                                </div>s
                            {% endfor %}
                        {% endfor %}
                     
                   
                </div>
            </div>
        </div>
        {{ form_end(form) }} 
        

                   
        </div> 
    </div>
    
<h1>Liste des types d'articles</h1>
{% if user %}
  <table id="user" class="table table-striped">
    <thead>
      <tr>
        <th>N°</th>
        <th>Type d'article</th>    
      </tr>
    </thead>
    <tbody>
      {% for user in user %}
      <tr>
            <td>{{ user.id }}</td>
            <td>{{ user.NomType }}</td>
          
            
            <td>
              <a href="/user/{{ user.id }}" class="btn btn-success">Modifier</a>
              <a class="btn btn-danger delete-user" data-toggle="modal" data-target="#exampleModal">Supprimer</a>
              <!-- Message de Confirmation -->

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title" id="exampleModalLabel" style="color: red;font-weight:bold">Suppression</h1>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <h3 style="font-weight:bold">Voulez-vous effectuer la suppression ?</h3>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal" style="font-size:20px">Annuler</button>
        <a href="/supprimerUser/{{ user.id }}"><button type="button" class="btn btn-primary" style="font-size:20px">Confirmer</button></a>
      </div>
    </div>
  </div>
</div>
            </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% else %}
  <p style="color: red;font-size:50px">La liste des Utilisateurs est vide pour l'instant</p>
{% endif %}
</div>
   
{% endblock %}

{% block javascript %}
   
{% endblock %}